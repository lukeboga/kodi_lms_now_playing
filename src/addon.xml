<addon id="plugin.audio.klmsaddon" version="0.0.1" name="KLMS Addon" provider-name="LukeBM">
    <!--
    The addon element defines the core metadata for the KLMS Addon.
    - id: A unique identifier for the addon. 'plugin.audio.klmsaddon' indicates that this is an audio plugin named 'klmsaddon'.
    - version: The version of the addon. Starting at '0.0.1' for the initial development phase.
    - name: The name of the addon, displayed in the Kodi interface.
    - provider-name: The author or provider of the addon.
    -->
    <requires>
        <!--
        The requires element specifies the dependencies for the addon.
        - import addon: The 'xbmc.python' addon is required for running Python scripts within Kodi.
        - version: The version of the xbmc.python addon required. Version '3.0.0' is compatible with Kodi Nexus 20.5.
        -->
        <import addon="xbmc.python" version="3.0.0"/>
        <!--
        Adding the requests module as a dependency to ensure it is installed when the addon is installed.
        - addon: The 'script.module.requests' package provides the requests library.
        - version: The minimum version of the requests module required.
        -->
        <import addon="script.module.requests" version="2.25.1"/>
    </requires>
    <extension point="xbmc.python.pluginsource" library="default.py">
        <!--
        The extension element defines the entry point for the addon.
        - point: The type of extension. 'xbmc.python.pluginsource' indicates a Python plugin source.
        - library: The main script file for the addon, which will be 'default.py'.
        -->
        <provides>audio</provides>
        <!--
        The provides element specifies the type of content the addon handles. In this case, it is an 'audio' plugin.
        -->
    </extension>
    <extension point="xbmc.addon.metadata">
        <!--
        The metadata for the addon, providing additional descriptive information.
        - summary: A brief summary of the addon. 'lang' attribute specifies the language (English).
        - description: A detailed description of the addon's functionality.
        - platform: Specifies the platforms the addon is compatible with. 'all' indicates compatibility with all platforms supported by Kodi.
        -->
        <summary lang="en">KLMS Addon</summary>
        <description lang="en">This add-on fetches and displays now playing information from a Logitech Media Server.</description>
        <platform>all</platform>
    </extension>
</addon>

